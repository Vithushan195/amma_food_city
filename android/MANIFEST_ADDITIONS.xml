<!-- 
  Add these to your existing android/app/src/main/AndroidManifest.xml

  Inside <manifest> tag (before <application>):
-->
<uses-permission android:name="android.permission.INTERNET"/>
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

<!--
  Inside <application> tag:
-->
<meta-data
    android:name="com.google.android.gms.wallet.api.enabled"
    android:value="true" />

<!--
  Inside the main <activity> tag, add this intent-filter for Stripe deep links:
-->
<intent-filter>
    <action android:name="android.intent.action.VIEW"/>
    <category android:name="android.intent.category.DEFAULT"/>
    <category android:name="android.intent.category.BROWSABLE"/>
    <data
        android:scheme="ammafoodcity"
        android:host="stripe-redirect"/>
</intent-filter>
